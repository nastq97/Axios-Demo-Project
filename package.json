{
  "dependencies": {
    "@faker-js/faker": "^7.6.0",
    "axios": "^1.1.3",
    "chai": "^4.3.6",
    "mocha": "^10.1.0",
    "mochawesome": "^7.1.3",
    "properties-reader": "^2.2.0",
    "rimraf": "^3.0.2",
    "shelljs": "^0.8.5"
  },
  "name": "axios",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "clean:reports": "",
    "pretest": "npm run clean:reports",
    "generate-report": "",
    "merge-reports": "",
    "post-test": "npm run merge-reports && npm run generate-reports",
    "test-dev": "BASE_URL='http://jsonplaceholder.typicode.com/' npm run .... || npm run posttest",
    "test-pre": "BASE_URL='http://jsonplaceholder.typicode.com/' npm run .... || npm run posttest",
    "test": "mocha --recursive Tests/**/*.test.ts BASE_URL='http://jsonplaceholder.typicode.com/'",
    "mochawesome": "npx test --timeout 10000 --reporter mochawesome",
    "lint": "eslint -c .eslintrc.js 'axios/**/*.ts' --fix"
    //add the script to check the linter as well
    //add pre-commit library
    //update script naming - make more descripting
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/mocha": "^10.0.0",
    "@types/node": "^18.11.9",
    "@typescript-eslint/eslint-plugin": "^5.41.0",
    "@typescript-eslint/parser": "^5.41.0",
    "eslint": "^8.26.0",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-prettier": "^4.2.1",
    "ts-node": "^10.9.1"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,jsx}": [
      "prettier --write",
      "eslint --fix",
      "git add"
    ],
    "*.{html,css,less,ejs}": [
      "prettier --write",
      "git add"
    ]
  }
}
